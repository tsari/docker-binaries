#!/bin/bash
sudo docker run -it --rm \
    -e "USER" \
    -e "UID=$(id -u)" \
    -e "GID=$(id -g)" \
    -e "HOME=$HOME" \
    -v /home/$USER:/home/$USER \
    -v /etc/machine-id:/etc-machine-id:ro \
    -v /etc/localtime:/etc/localtime:ro \
    -v $PWD:/app \
tsari/build-server composer "$@"